# Publishing and Installation

This Skill is designed for public GitHub-first publishing. A public repository makes the files easy to review, clone, share, and install with compatible Agent Skills tools.

## Publish on GitHub

1. Confirm the package contains `SKILL.md`, `README.md`, `LICENSE`, `CHANGELOG.md`, `SECURITY.md`, `CONTRIBUTING.md`, `docs/`, and `templates/`.
2. Search for private names, private paths, private projects, credentials, and local-only notes.
3. Add repository topics such as `agent-skills`, `chatgpt-skills`, `codex-skills`, `macos`, and `mac-migration`.
4. Use normal GitHub review before release.
5. If GitHub CLI supports Skill publishing in your environment, run its validation or publish command after reviewing what it will do.

## Install in ChatGPT

Where ChatGPT Skills are available, upload or share the Skill folder through the product's Skills flow. Availability depends on the user's plan and workspace settings.

Do not claim that an official public Skill marketplace exists unless the platform provides one.

## Install in Codex

Place the Skill folder in a project skill location or user skill location supported by Codex. Then invoke it by name:

```text
Use the Mac Migration Operator Skill to validate my new Mac before I retire the old one.
```

## Install With Compatible Tools

Agent Skills use an open folder format centered on `SKILL.md`. Compatible tools may install from a GitHub repository or local folder. Follow the specific tool's instructions and review what files it will add.

## Monetization Notes

Direct paid Skill sales are not the main official path today. Practical future paths may include a public GPT if eligible, a ChatGPT App if the platform fit is strong, consulting, paid support, or training material. Do not overstate platform support or promise payment options that do not exist.

Next step: before publishing, run a private-content search across the package and review every match manually.
